import 'package:flutter/material.dart';
class SatisticsPage extends StatelessWidget {
  const SatisticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Satistics Page'),),
    );
  }
}
